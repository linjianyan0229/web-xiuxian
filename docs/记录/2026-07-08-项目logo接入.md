# 项目 logo 接入（favicon + 三处品牌位）

- 日期：2026-07-08 01:20
- 需求：`web/image/logo.png`（2048×2048 透明底，青绿水墨「仙」徽章）作为项目 logo。

## 改动内容
- 后端：无
- 资源：sharp 压制两份——`web/public/favicon.png`（128px PNG，23KB，浏览器标签图标）、`web/image/logo.webp`（256px，24KB，界面用）；原 `logo.png`（2.7MB）留作素材源不进包。
- 前端接入四处：
  - `index.html`：favicon 由占位 `favicon.svg` 换为 `/favicon.png`（标题本就是「文字修仙」，未动）；
  - 登录/注册壳 `AuthShell.vue`：品牌区金印圆盘（金底「仙」字）换为 logo 图，保留外圈旋转金环动画，配青绿投影；
  - 首页 `HomeView.vue` 顶栏：标题左侧加 44px logo（flex 排布）；
  - 后台 `AdminLayout.vue` 侧边栏：色块「仙」印章换为 32px logo。

## 涉及文件
- 新增：`web/public/favicon.png`、`web/image/logo.webp`
- 修改：`web/index.html`、`web/src/components/AuthShell.vue`、`web/src/views/HomeView.vue`、`web/src/layouts/AdminLayout.vue`

## 验证
- 前端构建通过：logo.webp 进包 24.5KB，`dist/favicon.png` 正常复制且 `dist/index.html` 引用正确。

## 遗留
- `web/public/favicon.svg` 旧占位文件已无引用，暂保留；改原 logo 后需用 sharp 重新压制两份产物。
