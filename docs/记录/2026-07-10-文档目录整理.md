# 文档目录整理（设计文档/记录归置）

- 日期：2026-07-10 01:39
- 需求：把机制初稿与改动/设计记录统一归置到 `docs/` 下，并更新 CLAUDE.md 的目录约定。

## 改动内容
- 后端：无。
- 前端：无。
- 文档结构：
  - 新建 `docs/设计文档/`，将原 `资源文件/机制相关/` 下 8 份机制文档（修为修炼、境界突破、宗门、宗门职位与权限、悟性与修炼速度、打坐、探索、功法）全部迁入。原 `资源文件/机制相关/` 空目录删除。
  - `docs/改动记录/` 重命名为 `docs/记录/`（既有 8 份改动记录随之迁移）。
  - CLAUDE.md：开头「项目概述」补充 `docs/` 目录结构说明（技术文档/设计文档/记录三分）；「改动总结文档」路径由 `docs/改动记录/` 改为 `docs/记录/`；5 处机制设计文档引用路径由 `资源文件/机制相关/*.md` 改为 `docs/设计文档/*.md`。

## 涉及文件
- 迁移：`资源文件/机制相关/*.md`（8 份）→ `docs/设计文档/*.md`
- 重命名：`docs/改动记录/` → `docs/记录/`
- 修改：`CLAUDE.md`（目录结构说明 + 记录路径 + 机制文档路径）

## 验证
- `git status`：6 份原被跟踪的机制文档显示为 D（删除），因 `docs/` 已 gitignore，迁入后成为纯本地文档（符合「docs 不上传 GitHub」定位）；探索/功法为本会话新增、原未跟踪。
- 迁移后设计文档间的相对链接（同目录 `./xxx.md`）不受影响；grep 确认 CLAUDE.md 无残留 `资源文件/机制相关`、`docs/改动记录` 旧路径。

## 遗留
- 迁移使机制设计稿从 GitHub 仓库移除（转为本地文档），为预期效果。若日后希望设计稿仍进版本库，需另议（如取消 docs 的 gitignore 或单独放行 `docs/设计文档/`）。
